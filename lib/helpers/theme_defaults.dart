import 'package:flutter/material.dart';

// Mapping of theme modes with integers, these integers are then 
// used to identify the theme mode they represent
Map<int, ThemeMode> themeMap = {
  1: ThemeMode.system,
  2: ThemeMode.light,
  3: ThemeMode.dark,
};

